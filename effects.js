(()=>{let c,muted=localStorage.getItem("ddMuted")==="1";const q=s=>document.querySelector(s),btn=q("#v03sound"),pop=q("#v03pop"),slash=q("#v03slash"),crit=q("#v03crit");function ac(){if(!c)c=new(window.AudioContext||window.webkitAudioContext)();if(c.state==="suspended")c.resume();return c}function tone(f,d=.08,t="sine",v=.05,to=f){if(muted)return;let x=ac(),o=x.createOscillator(),g=x.createGain();o.type=t;o.frequency.setValueAtTime(f,x.currentTime);o.frequency.exponentialRampToValueAtTime(Math.max(30,to),x.currentTime+d);g.gain.setValueAtTime(v,x.currentTime);g.gain.exponentialRampToValueAtTime(.001,x.currentTime+d);o.connect(g);g.connect(x.destination);o.start();o.stop(x.currentTime+d)}function noise(d=.07,v=.035){if(muted)return;let x=ac(),b=x.createBuffer(1,x.sampleRate*d,x.sampleRate),a=b.getChannelData(0);for(let i=0;i<a.length;i++)a[i]=Math.random()*2-1;let n=x.createBufferSource(),g=x.createGain();n.buffer=b;g.gain.setValueAtTime(v,x.currentTime);g.gain.exponentialRampToValueAtTime(.001,x.currentTime+d);n.connect(g);g.connect(x.destination);n.start()}function replay(e,k){if(!e)return;e.classList.remove(k);void e.offsetWidth;e.classList.add(k)}btn.textContent=muted?"🔇":"🔊";btn.onclick=()=>{muted=!muted;localStorage.setItem("ddMuted",muted?"1":"0");btn.textContent=muted?"🔇":"🔊";if(!muted)tone(520,.09,"triangle",.05,650)};document.addEventListener("pointerdown",e=>{let d=e.target.closest(".die");if(!d)return;ac();replay(d,"rolling");let ds=[...document.querySelectorAll(".die")],i=ds.indexOf(d);noise(.05,.03);tone(i===2?150:220,.1,"square",.035,i===2?80:330);setTimeout(()=>tone(i===2?540:390,.08,"triangle",.05),90)},true);let last="";new MutationObserver(ms=>{for(let m of ms){let t=m.target.textContent||"",z=t.match(/(?:ダメージ|DAMAGE)[^\d]*(\d+)/i)||t.match(/(\d+)\s*(?:ダメージ|damage)/i);if(z&&t!==last){last=t;pop.textContent=z[1];replay(pop,"show");replay(slash,"show");noise(.1,.07);tone(120,.14,"sawtooth",.05,55)}}let all=document.body.innerText;if(/666/.test(all)&&/CRITICAL/i.test(all)&&!crit.classList.contains("show")){replay(crit,"show");tone(90,.18,"sawtooth",.08,45);setTimeout(()=>tone(440,.2,"square",.07,880),180);setTimeout(()=>tone(880,.25,"triangle",.08,1350),380)}}).observe(document.body,{subtree:true,childList:true,characterData:true});document.addEventListener("pointerdown",ac,{once:true,capture:true})})();